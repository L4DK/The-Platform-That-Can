/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { AiGatekeeperAgentService } from "../aiGatekeeperAgent.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { AiGatekeeperAgentCreateInput } from "./AiGatekeeperAgentCreateInput";
import { AiGatekeeperAgentWhereInput } from "./AiGatekeeperAgentWhereInput";
import { AiGatekeeperAgentWhereUniqueInput } from "./AiGatekeeperAgentWhereUniqueInput";
import { AiGatekeeperAgentFindManyArgs } from "./AiGatekeeperAgentFindManyArgs";
import { AiGatekeeperAgentUpdateInput } from "./AiGatekeeperAgentUpdateInput";
import { AiGatekeeperAgent } from "./AiGatekeeperAgent";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class AiGatekeeperAgentControllerBase {
  constructor(
    protected readonly service: AiGatekeeperAgentService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: AiGatekeeperAgent })
  @nestAccessControl.UseRoles({
    resource: "AiGatekeeperAgent",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async create(
    @common.Body() data: AiGatekeeperAgentCreateInput
  ): Promise<AiGatekeeperAgent> {
    return await this.service.create({
      data: data,
      select: {
        AIStatus: true,
        createdAt: true,
        id: true,
        roles: true,
        TasksCompleted: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [AiGatekeeperAgent] })
  @ApiNestedQuery(AiGatekeeperAgentFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "AiGatekeeperAgent",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findMany(@common.Req() request: Request): Promise<AiGatekeeperAgent[]> {
    const args = plainToClass(AiGatekeeperAgentFindManyArgs, request.query);
    return this.service.findMany({
      ...args,
      select: {
        AIStatus: true,
        createdAt: true,
        id: true,
        roles: true,
        TasksCompleted: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: AiGatekeeperAgent })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AiGatekeeperAgent",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async findOne(
    @common.Param() params: AiGatekeeperAgentWhereUniqueInput
  ): Promise<AiGatekeeperAgent | null> {
    const result = await this.service.findOne({
      where: params,
      select: {
        AIStatus: true,
        createdAt: true,
        id: true,
        roles: true,
        TasksCompleted: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: AiGatekeeperAgent })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AiGatekeeperAgent",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async update(
    @common.Param() params: AiGatekeeperAgentWhereUniqueInput,
    @common.Body() data: AiGatekeeperAgentUpdateInput
  ): Promise<AiGatekeeperAgent | null> {
    try {
      return await this.service.update({
        where: params,
        data: data,
        select: {
          AIStatus: true,
          createdAt: true,
          id: true,
          roles: true,
          TasksCompleted: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: AiGatekeeperAgent })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "AiGatekeeperAgent",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async delete(
    @common.Param() params: AiGatekeeperAgentWhereUniqueInput
  ): Promise<AiGatekeeperAgent | null> {
    try {
      return await this.service.delete({
        where: params,
        select: {
          AIStatus: true,
          createdAt: true,
          id: true,
          roles: true,
          TasksCompleted: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
